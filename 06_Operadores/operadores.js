// Operadores
/* 
Operadores Aritméticos:
    + Adição
    a + b
    - Subtração
    a - b
    * Multiplicação
    a * b
    / Divisão
    a / b
    % Resto da divisão
    a % b
    ** Potência
    a ** b
    ++ Incrementar
    a++
    -- Decrementar
    b--
            
Operadores Relacionais:
    var a = 8;
    var b = "8";
    == Igual a (valor)
    (a == b) = True
    === Identico a (valor e tipo)
    (a === b) = False 
    != Diferente de (valor)
    (a != b) = False 
    !== Diferente de identico a (valor e tipo)
    (a !== b) = True
    > Maior que
    (a > b) = False 
    >= Maior ou igual a
    (a >= b) = True 
    < Menor que
    (a < b) = False
    <= Menor ou igual a
    (a <= b) = True
    && E lógica
    (a == 8 && b == 8) = True 
    || OU lógica
    (a === 8 || b === 8) = True
    ! Negação lógica
    !(a === 10) = True

Operador Ternário
    condição ? true : false

Lambda
    Função de seta para somar dois números
const soma = (a, b) => a + b;

    É equivalente a:
function soma(a, b) {
  return a + b;
}

*/